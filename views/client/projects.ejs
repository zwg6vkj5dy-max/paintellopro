<% layout('layouts/main') %>
<h2>Your projects</h2>
<ul>
  <% projects.forEach(p => { %>
    <li>
      <strong><%= p.details.description %></strong>
      — Status: <%= p.status %>
      — Painter: <%= p.painter ? p.painter.name : 'Not matched yet' %>
    </li>
  <% }) %>
</ul>
